# work

运维与采集脚本（对应 GPT 会话 `work/` 目录）。

- `probe.py`：iFinD token + 四指数行情冒烟。
- 计划迁移：`collect.py`（概念目录+行情采集）、`collect_ifind_long_range.py`（2025 起长区间）、
  `collect_broad_targets.py`（13 宽基池）、`collect_exploration.py`（探索指数扩充）。
