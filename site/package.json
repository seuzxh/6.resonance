{
  "name": "resonance-site",
  "private": true,
  "scripts": {
    "docs:dev": "node scripts/sync-docs.mjs && vitepress dev .",
    "docs:build": "node scripts/sync-docs.mjs && vitepress build .",
    "docs:preview": "vitepress preview ."
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "dependencies": {
    "echarts": "^6.1.0"
  }
}
